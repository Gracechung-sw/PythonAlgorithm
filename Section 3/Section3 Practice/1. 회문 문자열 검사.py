N = int(input())

for(i in range(N)){
    word = input()

    word = word.lower()
    word_len = len(word)

    # i=0, j=word_len-1
    # while(i<=word_len/2){
    #     if(word[i] != word[j]){
            
    #     }
    #     else{

    #     }
    # } //이렿게 c와 다름없는 것을 짜려고했는데, 
    # ///python의 성질을 계속 사용해야 한다는 생각이 들었다. 
    # // python은 앞에서는  0, 1, 2 3, 순서이고, 
    # // 뒤에서는 -1. -2, -3 순서로 갈 수 있다. 

    for j in range(len//2):
        0+= 9+4
        10
        4+1110
        ''=

}

. 